package com.train.entity;import com.baomidou.mybatisplus.annotation.IdType;import com.baomidou.mybatisplus.annotation.TableId;import com.baomidou.mybatisplus.annotation.TableName;import io.swagger.v3.oas.annotations.media.Schema;import lombok.Data;import java.time.LocalDateTime;@Data@TableName("train_video")  // 对应数据库培训视频表public class TrainVideo {    @Schema(description = "视频ID")    @TableId(type = IdType.AUTO)  // 自增主键    private Long videoId;          // 视频ID（主键）    @Schema(description = "视频名称")    private String videoName;      // 视频名称（100位，即文件名）    @Schema(description = "缩略图路径")    private String coverUrl;       // 缩略图路径（255位）    @Schema(description = "播放地址")    private String playUrl;        // 播放地址（255位）    @Schema(description = "状态", example = "1")    private Integer status;        // 状态（0=禁用，1=启用，默认1）    @Schema(description = "创建时间")    private LocalDateTime createTime;  // 创建时间}