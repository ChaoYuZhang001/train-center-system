<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.train.mapper.SysMenuMapper">

    <!-- 根据菜单ID列表查询权限标识 -->
    <select id="selectPermsByMenuIds" resultType="java.lang.String">
        SELECT DISTINCT perms
        FROM sys_menu
        WHERE menu_id IN
        <foreach collection="menuIds" item="menuId" open="(" separator="," close=")">
            #{menuId}
        </foreach> AND perms IS NOT NULL AND perms != ''
    </select>

    <!-- 根据角色ID列表查询关联的权限标识 -->
    <select id="selectPermsByRoleIds" resultType="java.lang.String">
        SELECT DISTINCT m.perms
        FROM sys_role_menu rm
        LEFT JOIN sys_menu m ON rm.menu_id = m.menu_id
        WHERE rm.role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach> AND m.perms IS NOT NULL AND m.perms != ''
    </select>

    <select id="selectMenuListByRoleIds" resultType="com.train.entity.SysMenu" >
        SELECT DISTINCT m.*
        FROM sys_role_menu rm
        LEFT JOIN sys_menu m ON rm.menu_id = m.menu_id
        WHERE rm.role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
        ORDER BY m.sort ASC
    </select>

    <select id="selectAllEnableMenu" resultType="com.train.entity.SysMenu" >
        SELECT *        FROM sys_menu
        ORDER BY sort ASC
    </select>
</mapper>